# Environment variables for intellifunnel-console.
# Copy to .env.local for local development. No variables are required yet;
# database and service credentials are added starting with the DB setup task.
